{
  "name": "z7pdf-editor",
  "version": "1.0.0",
  "description": "A local PDF editor for merge, compress, resize, and rotate operations.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch-path=server.js --watch-path=db.js --watch-path=public --watch-path=routes --watch-path=services --watch-path=middleware --watch-path=utils server.js"
  },
  "dependencies": {
    "@napi-rs/canvas": "^0.1.53",
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.1",
    "helmet": "^8.1.0",
    "jszip": "^3.10.1",
    "multer": "^2.0.2",
    "nodemailer": "^8.0.4",
    "pdf-lib": "^1.17.1",
    "pdfjs-dist": "^4.10.38"
  }
}
